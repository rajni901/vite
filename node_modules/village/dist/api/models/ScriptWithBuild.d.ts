import type { Build } from './Build';
import type { Engine } from './Engine';
export declare type ScriptWithBuild = {
    id: string;
    name: string;
    created_at: string;
    updated_at: string;
    engine: Engine;
    builds?: Array<Build>;
    engine_version: string;
    description: string;
    workspace_id: string;
};
