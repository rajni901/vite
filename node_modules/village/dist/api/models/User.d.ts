import type { Build } from './Build';
import type { Run } from './Run';
import type { Schedule } from './Schedule';
import type { Script } from './Script';
import type { Workspace } from './Workspace';
import type { WorkspaceUsers } from './WorkspaceUsers';
/**
 * Represents a User record
 */
export declare type User = {
    id: string;
    created_at: string;
    updated_at?: string;
    created_builds?: Array<Build>;
    created_runs?: Array<Run>;
    created_schedules?: Array<Schedule>;
    created_scripts?: Array<Script>;
    created_workspaces?: Array<Workspace>;
    workspaces?: Array<WorkspaceUsers>;
};
