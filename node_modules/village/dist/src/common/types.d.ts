import { Engine, ParamType } from '../../api';
export interface Config {
    id: string;
    name: string;
    workspace: string;
    engine: Engine;
    engine_version: string;
    params: ParamType[];
}
